<%# need to add link to log in /sign in button. Insert the link after fixing the routes %>
<div class="home">
  <div class="center">
    <%= image_tag(asset_path('Logo.png')) %>
  </div>
  <div class="add-font">
     <% if user_signed_in? %>
        <%= link_to 'Back to the Game', user_characters_path(current_user), class:'btn btn-warning' %>
      <% else %>
        <%= link_to 'Log in / Sign in', new_user_session_path, class:"btn btn-warning" %>
      <% end %>

  </div>
</div>
